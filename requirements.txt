# Embedding path: use PyPI `birdnet` (Cornell) for TF 2.4 acoustic model; needs Python>=3.11 and compatible TensorFlow wheels.
# Do not pin an old tensorflow ceiling here — `birdnet` declares its own lower bound.

# Core tables + split + sklearn head
pandas>=2.0,<3
numpy>=1.24,<3
scikit-learn>=1.3,<2
PyYAML>=6,<7
joblib>=1.3,<2
scipy>=1.10,<2
tqdm>=4.66,<5

# Audio IO (BirdNET Analyzer 2.4.x requires librosa>=0.11)
librosa>=0.11,<0.13
soundfile>=0.12,<0.15

# BirdNET embeddings + optional synth verification: PyPI package `birdnet` (TensorFlow backend).
# Override device with env BIRDCLEF_BIRDNET_DEVICE=CPU|GPU:0 or cli_embed --birdnet-device GPU:0 (defaults to CPU).
# GPU issues (e.g. FusedBatchNorm / "No DNN support for stream"): unset GPU or BIRDCLEF_BIRDNET_FORCE_TF_CPU=1.
birdnet>=0.2.5,<1

# Scratch mel-CNN / ResNet18 training
torch>=2.2,<3
torchaudio>=2.2,<3
torchvision>=0.17,<1

# Part B synthetic audio (AudioLDM2 / "AudioLM2" in briefs)
diffusers>=0.27,<1
transformers>=4.40,<5
accelerate>=0.28,<1
safetensors>=0.4,<1
