# Embedding path: birdnet-analyzer 2.4.x needs Python>=3.11, librosa>=0.11, tensorflow>=2.20 (PyPI).
# Do not cap tensorflow below 2.20 if you pin birdnet-analyzer>=2.4.

# Core tables + split + sklearn head
pandas>=2.0,<3
numpy>=1.24,<3
scikit-learn>=1.3,<2
PyYAML>=6,<7
joblib>=1.3,<2
scipy>=1.10,<2
tqdm>=4.66,<5

# Audio IO (BirdNET Analyzer 2.4.x requires librosa>=0.11)
librosa>=0.11,<0.13
soundfile>=0.12,<0.15

# BirdNET embeddings: let birdnet-analyzer pull compatible tensorflow/birdnet.
# Avoid pinning tensorflow here — it clashes with Analyzer's transitive pins on some resolves.
birdnet-analyzer>=2.4,<4

# Scratch mel-CNN training
torch>=2.2,<3
torchaudio>=2.2,<3

# Part B synthetic audio (AudioLDM2 / "AudioLM2" in briefs)
diffusers>=0.27,<1
transformers>=4.40,<5
accelerate>=0.28,<1
safetensors>=0.4,<1
